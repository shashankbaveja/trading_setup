[SIMULATOR_SETTINGS]
# General settings for the TradingSimulator
index_token = 256265
initial_capital = 100000
option_data_fetch_buffer_minutes = 60
concurrent_signal_trade = false 
# If false, a new NIFTY BUY signal will be skipped if a trade from a previous signal is still considered active.

[DATA_PREP_DEFAULTS]
# Default parameters for indicators if not specifically overridden
# by a strategy's needs when calling calculate_statistics.
default_donchian_length_for_dp = 20
# default_rsi_window = 14

[STRATEGY_CONFIG_DonchianStandard]
# Configuration for a 'standard' Donchian Breakout strategy
strategy_class_name = DonchianBreakoutStrategy
# --- Strategy-specific indicator parameters ---
length = 20
exit_option = 1
# --- Strategy-specific trading parameters ---
trade_interval = minute
option_type = CE
trade_units = 75
# e.g., number of lots. Comment moved to its own line
profit_target_pct = 0.20
stop_loss_pct = 0.05
max_holding_period_minutes = 30

[STRATEGY_CONFIG_DonchianAggressive]
# Configuration for an 'aggressive' Donchian Breakout strategy
strategy_class_name = DonchianBreakoutStrategy
# --- Strategy-specific indicator parameters ---
length = 10
exit_option = 2
# --- Strategy-specific trading parameters ---
trade_interval = minute
option_type = CE
trade_units = 75
# e.g., number of lots. Comment moved to its own line
profit_target_pct = 0.25
stop_loss_pct = 0.08
max_holding_period_minutes = 20

# Example for a future strategy:
# [STRATEGY_CONFIG_RSIMeanReversion]
# strategy_class_name = RSIMeanReversionStrategy
# rsi_period = 14
# rsi_overbought = 70
# rsi_oversold = 30
# rsi_exit_on_opposite_signal = true 
# trade_interval = 5minute
# option_type = PE
# trade_units = 2
# profit_target_pct = 0.15
# stop_loss_pct = 0.07
# max_holding_period_minutes = 60
